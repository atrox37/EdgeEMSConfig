import { describe, it, expect } from 'vitest'

// 由于 common.ts 文件是空的，我们创建一个简单的测试
describe('Common Utils', () => {
  it('should be defined', () => {
    // 这是一个占位符测试，因为 common.ts 文件目前是空的
    expect(true).toBe(true)
  })
})
